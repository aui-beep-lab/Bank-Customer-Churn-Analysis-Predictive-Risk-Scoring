# Bank Customer Churn — Prediction, EDA & Risk-Scoring Dashboard

End-to-end churn analytics project in R, built around the CRISP-DM framework:
data cleaning → exploratory analysis → multi-model classification → predictive
risk scoring → interactive Shiny dashboard.

## Dataset

"Bank Customer Churn Prediction" — 10,000 customer records with the following raw columns:

| Column             | Description                                   |
|--------------------|-----------------------------------------------|
| `customer_id`      | Unique customer identifier                    |
| `credit_score`     | Credit score                                  |
| `country`          | Customer's country (France / Germany / Spain) |
| `gender`           | Male / Female                                 |
| `age`              | Age in years                                  |
| `tenure`           | Years as a customer                           |
| `balance`          | Account balance                               |
| `products_number`  | Number of bank products held                  |
| `credit_card`      | Has a credit card (0/1)                       |
| `active_member`    | Active member flag (0/1)                      |
| `estimated_salary` | Estimated annual salary                       |
| `churn`            | Target — 1 = churned, 0 = retained            |

The raw CSV is not committed to this repo (see `.gitignore`). Place your
copy anywhere convenient and point `raw_path` at the top of
`Bank_Churn_Full_Analysis.R` (or `scripts/01_data_cleaning.R`) at it by
default it looks for `Bank Customer Churn Prediction.csv` in the same folder
as the script.

## Two ways to run this

- **`Bank_Churn_Full_Analysis.R`** — everything (cleaning → EDA → modeling →
  risk scoring) combined into **one single script**, run top to bottom.
  Use this if you just want one file to point to on GitHub.
- **`scripts/00_run_pipeline.R` + `scripts/01-04`** the same logic split
  into modular, individually-rerunnable files. Use this if you want to
  re-run just one stage (e.g. re-score risk without re-training models).

Both produce identical output into `data/processed/`, `models/`, and
`outputs/`. The Shiny dashboard (`dashboard/app.R`) and the knittable report
(`report/Bank_Churn_Analysis.Rmd`) work with either, since they just read
from those output folders.

## Project structure

```
Bank-Customer-Churn/
├── Bank_Churn_Full_Analysis.R    # single-file version of the whole pipeline
├── Bank Customer Churn Prediction.csv   # put your raw CSV here (git-ignored)
├── data/
│   └── processed/             # cleaned data, generated by scripts/01
├── scripts/
│   ├── 00_run_pipeline.R     # runs everything end-to-end
│   ├── 01_data_cleaning.R    # CRISP-DM: data understanding + preparation
│   ├── 02_eda.R              # CRISP-DM: exploratory data analysis
│   ├── 03_modeling.R         # CRISP-DM: modeling + evaluation (5 models)
│   └── 04_risk_scoring.R     # predictive churn risk scoring ("forecasting")
├── models/                   # saved trained models (generated)
├── outputs/
│   ├── figures/              # all plots, generated
│   └── tables/               # all summary tables (CSV), generated
├── report/
│   └── Bank_Churn_Analysis.Rmd  # knittable narrative report (Word/HTML)
├── dashboard/
│   └── app.R                 # Shiny dashboard
├── .gitignore
└── README.md
```

## How to run

```r
# 1. Install deps + run the full pipeline
install.packages(c("tidyverse", "janitor", "caret", "randomForest", "xgboost",
                    "rpart", "pROC", "corrplot", "scales", "patchwork",
                    "shiny", "shinydashboard", "DT", "plotly"))

setwd("path/to/Bank-Customer-Churn")   # project root
source("scripts/00_run_pipeline.R")

# 2. Launch the dashboard
shiny::runApp("dashboard")

# 3. (Optional) Knit the narrative report
rmarkdown::render("report/Bank_Churn_Analysis.Rmd")
```

## Methodology

1. **Data cleaning** — type coercion, duplicate removal, median/mode
   imputation, derived features (`age_group`, `tenure_group`).
2. **EDA** — churn rate cuts by country, gender, age, tenure, product count,
   activity status, credit card ownership; correlation analysis vs. churn.
3. **Modeling** — Random Forest, XGBoost, Decision Tree, Logistic Regression,
   and KNN, each tuned with 5-fold cross-validation, compared on Accuracy,
   Kappa, Sensitivity, Specificity, and AUC.
4. **Risk scoring** — the best model (by AUC) scores every customer with a
   churn probability, bucketed into Low/Medium/High risk tiers, plus a
   decile/lift analysis showing how much value a retention team gets from
   acting on the highest-risk decile vs. random targeting.

   > **Why "risk scoring" instead of "forecasting"?** This dataset has no time
   > dimension (no dates, no repeated observations per customer), so a
   > classical time-series forecast (ARIMA, ETS, etc.) doesn't apply. The
   > standard substitute used across churn-analytics portfolios is predictive
   > risk scoring — ranking *current* customers by their probability of
   > churning next, which is what `04_risk_scoring.R` and the dashboard's
   > "Risk Scoring" and "What-If Predictor" tabs do.
5. **Dashboard** — five tabs: Overview (KPIs), Explore (filterable EDA),
   Model Comparison, Risk Scoring (with lift chart + downloadable list), and
   a What-If Predictor for scoring a hypothetical customer profile live.

## Key findings

*(Fill in after running the pipeline on the full dataset — placeholders below
reflect typical patterns on this dataset and should be replaced with your
actual numbers from `outputs/tables/`.)*

- Overall churn rate: **~20%**
- Churn is markedly higher in **Germany** than France/Spain
- Customers with **3–4 products** and **inactive members** churn at far
  higher rates
- Older customers (**>50**) churn more than younger cohorts
- Best model by AUC: *(see `outputs/tables/model_comparison.csv`)*

## Tech stack

R, tidyverse, caret, randomForest, xgboost, rpart, pROC, corrplot, shiny,
shinydashboard, plotly, DT.
